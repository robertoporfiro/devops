AzureMetrics
| where TimeGenerated > ago(1d)
| where ResourceGroup == "SAJINSIGHTS-MPMS" 
| where ResourceProvider == "MICROSOFT.ANALYSISSERVICES" 
| where Type == "AzureMetrics"
| where MetricName in ("memory_metric") 
| sort by TimeGenerated desc